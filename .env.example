# Copy to .env and fill in. .env is gitignored; this template is not.
# Only the keys for providers you actually run are required.

OPENAI_API_KEY=
DEEPSEEK_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
